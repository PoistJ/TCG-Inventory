<h2><%= card.card_name %></h2>

<form action="/MTG/<%= card.card_id %>/update" method="POST">
  <input type="number" name="card_id" value="<%= card.card_id %>" readonly />
  <input type="text" name="card_name" value="<%= card.card_name %>" />
  <input type="text" name="card_cost" value="<%= card.cost %>" />
  <select name="card_rarity" value="<%= card.rarity %>">
    <option value="common">Common</option>
    <option value="uncommon">Uncommon</option>
    <option value="rare">Rare</option>
    <option value="mythic">Mythic</option>
  </select>
  <input type="number" name="card_quantity" value="<%= card.quantity %>" />

  <input type="submit" />
</form>

<a href="/MTG/<%= card.card_id %>">Back</a>
